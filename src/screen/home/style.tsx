import {StyleSheet} from 'react-native';

export const createStyle = () => {
  return StyleSheet.create({
    mainContainer: {flex: 1, backgroundColor: 'red'},
    buttonContainer: {backgroundColor: 'orrange', padding: 100},
  });
};
